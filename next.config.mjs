const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "pinecone-academy-multi-step-form.vercel.app",
      },
    ],
  },
};

export default nextConfig;